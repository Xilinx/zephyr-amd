sample:
  name: DHCP Client
tests:
  sample.net.dhcpv4_client:
    depends_on: netif
    tags:
      - net
      - http
    harness: console
    harness_config:
      type: one_line
      regex:
        - "Lease time\\[\\d+\\]: \\d+ seconds"
